<h1>Books</h1>

<% @books.each do |book| %>
<div>
  <p><%= image_tag 'no_image.jpg' %><br>
     <%= book.user.name %></p>
  <p>Title</a>
  <sapn><%= link_to book.title, book_path(book.id) %></sapn>
  <p>Opsioin:<%= book.body %></p>
</div>
<% end %>
